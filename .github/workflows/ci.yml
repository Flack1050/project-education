name: CI

on:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on: self-hosted

    steps:
      - name: Check runner
        run: |
          echo "Runner works!"
          whoami
          hostname
          uname -a
          docker --version
